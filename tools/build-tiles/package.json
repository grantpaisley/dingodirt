{
  "name": "dingo-build-tiles",
  "private": true,
  "type": "module",
  "description": "Build and verify the shared AU basemap archive. Own package.json so the repo root and the no-build apps stay dependency-free (same reason as tools/ui-sweep).",
  "scripts": {
    "probe": "node probe-places.mjs"
  },
  "dependencies": {
    "pmtiles": "^4.4.1"
  }
}
